export enum SampleE {
  VALUE_A = 'Value A',
  VALUE_B = 'Value B',
}

export enum EnumRequestMethod {
  GET = 'GET',
  POST = 'POST',
}

export enum EnumAuthProvider {
  CREDENTIALS = 'credentials',
}
