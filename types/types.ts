export type SampleAT = {
  a: string;
  b: number;
};

export type SampleBT = [a: string, b: number];

export type TPost = {
  date: Date | string;
  image: string;
  slug: string;
  text: string;
  title: string;
};
